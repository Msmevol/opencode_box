{
    "target": "C:\\project\\box_opencode_2\\build\\test_target.exe",
    "args": "",

    "resources": {
        "memory_limit_mb": 256,
        "cpu_rate_percent": 50,
        "max_processes": 2
    },

    "allowed_dirs": [
        {
            "path": "C:\\project\\box_opencode_2",
            "writable": true
        }
    ],

    "domain_whitelist": [
        "*.github.com",
        "api.openai.com",
        "localhost"
    ],

    "registry_rules": [
        {
            "pattern": "HKCU\\Software\\**",
            "action": "allow"
        },
        {
            "pattern": "HKLM\\**",
            "action": "deny"
        },
        {
            "pattern": "*",
            "action": "deny"
        }
    ],

    "process_creation": "deny"
}
